/**
 * Erstelle eine Funktion "reverseArray(arr)", 
 * welche die Reihenfolge der Werte eines  
 * Arrays umkehrt.
 */

let arr = [1, 2, 3];
let revArr = reverseArray(arr);
console.log(revArr);
//OUTPUT: [3, 2, 1]

